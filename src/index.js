'use strict';

const { Client } = require('./client.model');
const { fieldData, recordId } = require('./utilities.service');

/**
 * @module Client
 * @module fieldData
 * @module recordId
 */

module.exports = {
  Client,
  fieldData,
  recordId
};

'use strict';

const {
  fieldData,
  recordId,
  namespace,
  filterResponse,
  sanitizeParameters
} = require('filemaker.utilities');

module.exports = {
  fieldData,
  recordId,
  namespace,
  filterResponse,
  sanitizeParameters
};

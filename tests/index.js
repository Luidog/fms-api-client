const assert = require('assert');
const { should, expect } = require('chai');

const { Filemaker } = require('../filemaker');

describe('FileMaker Data API Node Adapter', () => {
	it('will eventually have tests', () => {
		assert.equal(true, true);
	});
});
